[
    {
        "name": "generateid-dev",
        "image": "rorcommunity/generate-id",
        "cpu": 256,
        "memory": 512,
        "networkMode": "awsvpc",
        "essential": true,
        "portMappings": [
            {
                "containerPort": 80,
                "hostPort": 80
            }
        ],
        "logConfiguration": {
            "logDriver": "awslogs",
            "options": {
                "awslogs-group": "/ecs/generateid-dev",
                "awslogs-region": "eu-west-1",
                "awslogs-stream-prefix": "ecs"
            }
        },
        "environment": [
            {
                "name": "TOKEN",
                "value": "${token_dev}"
            },
            {
                "name": "ROUTE_USER",
                "value": "${route_user}"
            },
            {
                "name": "ROR_API_URL",
                "value": "${ror_api_url_dev}"
            },
            {
                "name": "ALLOWED_ORIGINS",
                "value": "${allowed_origins_dev}"
            },
            {
                "name": "GITHUB_VERSION",
                "value": "${version}"
            }
        ]
    }
]